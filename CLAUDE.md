# Claude 工作指南

> 仔细思考并实施最简洁的解决方案，尽可能少地更改代码。您将用中文回答所有问题。

---

## 📋 项目配置

**此框架为通用多代理协同开发框架**，可用于任何项目。

建议在你的项目根目录的 `CLAUDE.md` 文件中添加以下项目特定配置：
- 环境说明
- 项目信息和架构
- 项目目录结构
- 项目编码规范
- 技术架构文档
- 项目特定约定

---

## 核心工作流

> **重要变更**：主代理现在直接调度所有子代理

### 工作流概述

当你收到编码需求时，作为**主代理**，你应该**直接执行**以下完整工作流：

```
主代理 → 阶段0：创建会话
       → 阶段1：检查项目信息
       → 阶段2：并行调用 issue-analyzer（每个项目）
       → 阶段3：调用 analysis-aggregator 汇总
       → 阶段4：调用 master-planner 制定计划
       → 阶段5：等待用户确认（必须）
       → 阶段6：调用 plan-splitter 拆分任务
       → 阶段7-N：任务执行循环（code-executor → test-runner → code-auditor → task-summarizer）
```

### 关键原则

1. **主代理直接调度** - 你作为主代理，直接使用 Task 工具调用各个子代理，不通过中间层
2. **必须等待用户确认** - 在 master-planner 阶段完成后，必须等待用户批准才能继续
3. **完整质量保证** - 每个任务都必须通过测试和审计
4. **串行执行阶段** - 各阶段必须按顺序执行，不可跳过

---

## 🤖 工作流启动决策

**核心原则：通过 Plan Mode 让用户明确选择执行方式**

### 决策流程

当收到编码相关需求时，主代理按以下流程决策：

```
用户需求
  ↓
主代理初步分析
  ↓
是否为明显的非编码任务？
  ├─ YES（问答/查询） → 直接回答
  └─ NO（涉及代码修改） → 进入 Plan Mode
                           ↓
                   向用户展示任务分析和选项
                           ↓
                   用户选择执行方式
                           ↓
            ┌──────────────┴──────────────┐
            ↓                              ↓
    选项1：完整工作流              选项2：快速执行
    （推荐用于复杂任务）          （适用于简单修改）
```

### 使用 Plan Mode 询问用户

**对于所有涉及代码修改的任务**，使用 `EnterPlanMode` 工具，在 Plan Mode 中：

1. **展示任务分析**
   - 任务类型：新功能 / Bug修复 / 重构 / 配置调整
   - 影响范围：涉及的文件、模块、项目
   - 复杂度评估：简单 / 中等 / 复杂

2. **提供执行方式选项**（使用 `AskUserQuestion`）

```python
AskUserQuestion(
    questions=[
        {
            "question": "请选择执行方式？",
            "header": "执行方式",
            "multiSelect": false,
            "options": [
                {
                    "label": "完整工作流（推荐）",
                    "description": "包含需求分析、计划制定、代码实现、测试验证、代码审计。适合复杂任务和多文件修改。"
                },
                {
                    "label": "快速执行",
                    "description": "直接修改代码，跳过分析和计划阶段。仅适用于简单的单文件修改、配置调整或文档编辑。"
                }
            ]
        }
    ]
)
```

3. **根据用户选择执行**
   - **完整工作流** → 执行阶段0开始的完整流程（详见 [WORKFLOW.md](.claude/docs/CLAUDE/WORKFLOW.md)）
   - **快速执行** → 直接修改代码，但仍需通过测试

### 关键要点

1. **始终使用 Plan Mode** - 对于所有涉及代码修改的任务
2. **让用户选择** - 不要替用户做决定
3. **提供建议** - 根据复杂度推荐合适的方式
4. **保持灵活** - 用户可以选择任何方式
5. **质量优先** - 即使快速执行也要确保基本质量

---

## 📚 详细文档

### 工作流详细步骤
完整的工作流阶段说明（阶段0-N）请参考：[WORKFLOW.md](.claude/docs/CLAUDE/WORKFLOW.md)

包含内容：
- 会话创建步骤
- 项目信息检查
- 需求分析流程
- 计划制定与确认
- 任务拆分与执行
- 多代理系统说明

### 开发规范与约定
全局约束、编码规范和工作风格请参考：[CONVENTIONS.md](.claude/docs/CLAUDE/CONVENTIONS.md)

包含内容：
- 开发哲学（错误处理、测试原则）
- 工作风格约定
- 绝对规则（代码质量、架构、安全）
- Git 提交规范
- 计划管理规范
- 重要约定和注意事项

---

**框架版本**：2.0.0
**更新时间**：2026-01-09
**架构**：主代理直接调度，11个子代理协同
