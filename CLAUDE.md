# Claude 工作指南

> 仔细思考并实施最简洁的解决方案，尽可能少地更改代码。您将用中文回答所有问题。

---

## 📋 项目配置

**此框架为通用多代理协同开发框架**，可用于任何项目。

建议在你的项目根目录的 `CLAUDE.md` 文件中添加以下项目特定配置：
- 环境说明
- 项目信息和架构
- 项目目录结构
- 项目编码规范
- 技术架构文档
- 项目特定约定

---

## 核心工作流

> **重要变更**：主代理现在直接调度所有子代理

### 工作流概述

当你收到编码需求时，作为**主代理**，你应该**直接执行**以下完整工作流：

```
主代理 → 阶段0：创建会话
       → 阶段1：检查项目信息
       → 阶段2：并行调用 issue-analyzer（每个项目）
       → 阶段3：调用 analysis-aggregator 汇总
       → 阶段4：调用 master-planner 制定计划
       → 阶段5：等待用户确认（必须，唯一确认点）
       → 阶段6：调用 plan-splitter 拆分任务
       → 阶段7-N：任务执行循环（自动连续执行，无需用户确认）
         ├─ code-executor（实现业务代码）
         ├─ test-code-writer（创建测试代码）
         ├─ test-runner（运行测试）
         ├─ code-auditor（代码审计）
         └─ task-summarizer（自动继续下一任务）
```

### 关键原则

1. **主代理直接调度** - 你作为主代理，直接使用 Task 工具调用各个子代理，不通过中间层
2. **必须等待用户确认** - 在 master-planner 阶段完成后，必须等待用户批准才能继续
3. **一次确认，自动执行** - 用户在阶段5批准计划后，所有任务自动连续执行直到完成或失败
4. **禁止中间询问** - 在阶段7-N的任务执行循环中，除非遇到无法自动处理的错误，否则禁止询问用户是否继续
5. **完整质量保证** - 每个任务都必须通过测试和审计
6. **串行执行阶段** - 各阶段必须按顺序执行，不可跳过

---

## 🤖 工作流启动决策

**核心原则：在执行前让用户明确选择执行方式**

### 决策流程

当收到编码相关需求时，主代理按以下流程决策：

```
用户需求
  ↓
主代理初步分析（不进入 Plan Mode）
  ↓
是否为明显的非编码任务？
  ├─ YES（问答/查询） → 直接回答
  └─ NO（涉及代码修改） → 展示任务分析
                           ↓
                   使用 AskUserQuestion 询问执行方式
                           ↓
                   用户选择执行方式
                           ↓
            ┌──────────────┼──────────────┐
            ↓              ↓               ↓
    选项1：完整工作流  选项2：快速规划  选项3：直接执行
    启动阶段0         进入Plan Mode    读取计划直接执行
    （不用Plan Mode） 规划后执行       （不规划，不用Plan Mode）
```

### 步骤说明

#### 步骤 1：主代理初步分析

收到涉及代码修改的任务时，**在不进入 Plan Mode 的情况下**，分析并向用户展示：

1. **任务类型**：新功能 / Bug修复 / 重构 / 配置调整
2. **影响范围**：涉及的文件、模块、项目数量
3. **复杂度评估**：简单 / 中等 / 复杂

#### 特殊场景：用户提供外部计划文件

当用户提供外部计划文件路径时（如 `开始执行计划 /path/to/plan.md`）：

**正确处理方式**：
1. ✅ 读取该文件内容作为需求描述
2. ✅ 在 `.claude/sessions/` 下创建**新的独立会话目录**
3. ✅ 所有会话文件都写入新会话目录

**严格禁止**：
- ❌ 禁止在输入文件所在目录下创建任何会话文件
- ❌ 禁止使用 `os.path.dirname(input_file)` 作为会话目录
- ❌ 禁止将输入文件路径与会话工作目录混淆

**关键概念区分**：
- **输入文件路径**：用户提供的计划文件位置（只读，获取需求）
- **会话工作目录**：`.claude/sessions/{session_id}/`（读写，存储所有会话数据）

#### 步骤 2：询问用户选择执行方式

使用 `AskUserQuestion` 工具询问用户（**注意：此时仍未进入 Plan Mode**）：

```python
AskUserQuestion(
    questions=[
        {
            "question": "请选择执行方式？",
            "header": "执行方式",
            "multiSelect": false,
            "options": [
                {
                    "label": "完整工作流（推荐）",
                    "description": "包含需求分析、计划制定、代码实现、测试验证、代码审计。适合复杂任务和多文件修改。"
                },
                {
                    "label": "快速规划后执行",
                    "description": "使用 Plan Mode 规划后直接修改代码。仅适用于简单的单文件修改、配置调整或文档编辑。"
                },
                {
                    "label": "直接执行现有计划",
                    "description": "不重新规划，直接执行用户提供的计划文件或需求描述。适用于已有明确计划的场景。"
                }
            ]
        }
    ]
)
```

#### 步骤 3：根据用户选择执行

- **选项1：完整工作流**
  - **不进入 Plan Mode**
  - 直接启动阶段0开始的完整流程（详见 [WORKFLOW.md](.claude/docs/CLAUDE/WORKFLOW.md)）
  - 创建会话目录：`.claude/sessions/{session-id}/`
  - 通过子代理系统完成：issue-analyzer → master-planner → plan-splitter → code-executor
  - 包含完整的质量保证流程：测试验证 + 代码审计

- **选项2：快速规划后执行**
  - 使用 `EnterPlanMode` 工具进入 Plan Mode
  - 在 Plan Mode 中规划实施方案
  - 使用 `ExitPlanMode` 退出后**等待用户批准计划**

  **【关键】ExitPlanMode 后的状态管理（避免死循环）**：
  1. ✅ 主代理内部记住：**当前处于快速执行模式，等待计划批准**
  2. ✅ 向用户展示计划内容并等待回复
  3. ✅ 当用户回复时，**优先识别为计划批准信号**，而非新任务：
     - 包含"执行"、"开始"、"批准"、"继续"、"直接执行"等词
     - 或者是简短的肯定回复（"好"、"可以"、"是"、"OK"）
  4. ✅ 识别为批准后，**立即使用默认工具执行计划**
  5. ❌ **严格禁止**：把批准信号误判为新的编码需求
  6. ❌ **严格禁止**：在批准后重新询问"选择执行方式"
  7. ❌ **严格禁止**：在批准后重新进入 Plan Mode

  **避免循环的判断规则**：
  - 如果上一步操作是 `ExitPlanMode`
  - 且用户回复是简短指令（通常 ≤10个字）
  - → **这是计划批准，不是新任务请求**
  - → **直接执行，不要重新分析或询问**

  **快速规划的特点**：
  - ✅ **完全跳过工作流步骤** - 不创建会话目录，不进入工作流
  - ✅ 使用默认工具（Read、Edit、Write、Bash等）直接修改代码
  - ✅ 适合简单的单文件修改、配置调整或文档编辑
  - ❌ **不调用工作流中的任何子代理**：
    - 不调用 `issue-analyzer`、`analysis-aggregator`（不进行需求分析）
    - 不调用 `master-planner`、`plan-splitter`（不制定正式计划）
    - 不调用 `code-executor`（直接使用默认工具）
    - 不调用 `test-runner`、`test-code-writer`（不自动化测试）
    - 不调用 `code-auditor`、`auto-fixer`（不进行审计和自动修复）
    - 不调用 `task-summarizer`、`project-info-updater`（不更新会话）
  - ⚠️ 质量保证由主代理手动控制，适合低风险变更
  - ⚠️ 不生成工作流文档和执行报告
  - ⚠️ **用户批准计划后立即执行，不再询问或分析**

- **选项3：直接执行现有计划**
  - **不进入 Plan Mode** - 跳过规划阶段
  - **不调用任何工作流子代理** - 完全使用默认工具
  - **读取用户计划** - 如果用户提供计划文件路径，读取文件内容；否则使用用户的需求描述作为计划
  - **直接执行** - 立即使用 Read、Edit、Write、Bash 等默认工具执行
  - **不询问确认** - 用户选择此选项即表示批准执行

  **直接执行的执行流程**：
  1. ✅ 读取用户提供的计划文件（如果有路径）或使用用户描述
  2. ✅ 解析计划中的任务步骤
  3. ✅ 按照计划逐步执行，使用默认工具修改代码
  4. ✅ 执行完成后向用户汇报结果
  5. ❌ **严格禁止**：重新进入 Plan Mode
  6. ❌ **严格禁止**：询问用户是否执行或重新选择方式
  7. ❌ **严格禁止**：调用任何工作流子代理

  **直接执行的特点**：
  - ✅ **最快执行路径** - 无需分析、规划，直接执行
  - ✅ **适用场景**：
    - 用户提供了详细的计划文件
    - 用户明确说明"开始执行计划"、"直接执行"等指令
    - 用户已经清楚知道要做什么，不需要额外规划
  - ✅ 使用默认工具（Read、Edit、Write、Bash等）直接修改代码
  - ✅ 不创建会话目录，不生成工作流文档
  - ❌ **不调用工作流中的任何子代理**
  - ⚠️ 无自动化测试和代码审计
  - ⚠️ 质量保证完全由主代理手动控制
  - ⚠️ 适合低风险、明确需求的变更

### 关键要点

1. **先询问，再决定** - 不要先进入 Plan Mode，先询问用户选择
2. **完整工作流不用 Plan Mode** - 完整工作流通过子代理系统完成
3. **快速规划才用 Plan Mode** - 选项2使用 Plan Mode 进行规划
4. **直接执行最快路径** - 选项3跳过所有规划，直接执行现有计划
5. **让用户选择** - 不要替用户做决定
6. **提供建议** - 根据复杂度推荐合适的方式（在选项标签中标注"推荐"）
7. **质量优先** - 即使快速执行也要确保基本质量
8. **状态记忆（避免循环）** - ExitPlanMode 后的第一个用户回复优先识别为计划批准，不要重新触发决策流程
9. **识别直接执行场景** - 当用户说"开始执行计划"、"直接执行"等，优先推荐选项3

### 三种模式的对比

| 对比项 | 完整工作流 | 快速规划 | 直接执行 |
|--------|-----------|---------|---------|
| 适用场景 | 复杂任务、多文件修改 | 简单修改、配置调整 | 已有计划、明确需求 |
| 工作方式 | 多代理协同（12个代理） | Plan Mode + 默认工具 | 默认工具直接执行 |
| 会话目录 | 创建 `.claude/sessions/` | 不创建会话目录 | 不创建会话目录 |
| 需求分析 | issue-analyzer（深度分析） | 主代理简单分析 | 不分析（使用现有计划） |
| 计划制定 | master-planner（结构化计划） | Plan Mode规划 | 不规划（使用现有计划） |
| 代码实现 | code-executor（业务代码专用） | 默认工具（Read/Edit/Write） | 默认工具（Read/Edit/Write） |
| 测试创建 | test-code-writer（自动创建测试） | 不创建测试 | 不创建测试 |
| 测试运行 | test-runner（自动运行测试） | 不运行测试 | 不运行测试 |
| 代码审计 | code-auditor（质量审计） | 不进行审计 | 不进行审计 |
| 自动修复 | auto-fixer（自动修复问题） | 不自动修复 | 不自动修复 |
| 进度跟踪 | task-summarizer（自动更新） | 无进度跟踪 | 无进度跟踪 |
| 项目信息 | project-info-updater（自动更新） | 不更新 | 不更新 |
| 质量保证 | 完整（测试+审计） | 基本（手动控制） | 基本（手动控制） |
| 用户确认 | 1次（计划批准后自动执行） | 2次（Plan批准+执行确认） | 0次（选择即执行） |
| 执行报告 | 生成详细报告和文档 | 不生成报告 | 不生成报告 |
| 子代理调用 | 调用工作流中的12个子代理 | 不调用任何工作流子代理 | 不调用任何工作流子代理 |

---

## 📚 详细文档

### 工作流详细步骤
完整的工作流阶段说明（阶段0-N）请参考：[WORKFLOW.md](.claude/docs/CLAUDE/WORKFLOW.md)

包含内容：
- 会话创建步骤
- 项目信息检查
- 需求分析流程
- 计划制定与确认
- 任务拆分与执行
- 多代理系统说明

### 开发规范与约定
全局约束、编码规范和工作风格请参考：[CONVENTIONS.md](.claude/docs/CLAUDE/CONVENTIONS.md)

包含内容：
- 开发哲学（错误处理、测试原则）
- 工作风格约定
- 绝对规则（代码质量、架构、安全）
- Git 提交规范
- 计划管理规范
- 重要约定和注意事项

---

**框架版本**：2.4.0
**更新时间**：2026-01-23
**架构**：主代理直接调度，12个子代理协同
**更新说明**：
- 增加"直接执行现有计划"选项（选项3）
- 支持用户提供计划文件或明确需求时，跳过规划阶段直接执行
- 优化工作流决策逻辑，提供三种执行方式供用户选择
- 更新对比表，清晰展示三种模式的差异
