# Hook 配置优化说明

## 优化时间
2026-01-07

## 问题背景
启动时出现 `SessionStart:startup hook error`，原因是 SessionStart、Stop、SubagentStop 三个 hook 的 prompt 过长（超过 2000 行），导致 JSON 解析失败。

## 解决方案
采用**混合方案（方案A）**：
- ✅ **保留 Prompt Hook**：SessionStart、Stop、SubagentStop（简化版）
- ✅ **保留命令型 Hook**：PreToolUse、PostToolUse、UserPromptSubmit
- ✅ **大幅简化 prompt**：从 2000+ 字符减少到 200-400 字符

---

## 配置对比

### SessionStart Hook

**原配置**：
- Prompt 长度：~2000 字符
- 超时：30 秒
- 问题：解析失败

**新配置**：
- Prompt 长度：~250 字符
- 超时：15 秒
- 功能：检测工作流会话，提示继续执行

### Stop Hook

**原配置**：
- Prompt 长度：~3000 字符
- 超时：30 秒
- 包含详细的阶段判断逻辑

**新配置**：
- Prompt 长度：~350 字符
- 超时：20 秒
- 功能：简化的工作流状态检查和退出判断

### SubagentStop Hook

**原配置**：
- Prompt 长度：~500 字符
- 超时：30 秒

**新配置**：
- Prompt 长度：~200 字符
- 超时：20 秒
- 功能：验证子代理职责完成情况

---

## 功能保留情况

| 功能 | 原版本 | 新版本 | 状态 |
|------|--------|--------|------|
| 工作流状态检测 | ✅ | ✅ | 保留（简化） |
| 会话恢复提示 | ✅ | ✅ | 保留 |
| 停止拦截 | ✅ | ✅ | 保留（简化） |
| 子代理完成性检查 | ✅ | ✅ | 保留 |
| 详细阶段指导 | ✅ | ⚠️ | 简化 |
| 子代理验证 | ✅ | ✅ | 保留（命令型） |
| 进度跟踪 | ✅ | ✅ | 保留（命令型） |
| 文件追踪 | ✅ | ✅ | 保留（命令型） |
| 用户批准捕获 | ✅ | ✅ | 保留（命令型） |

---

## 简化的权衡

### 保留的核心功能 ✅
1. 工作流会话检测和恢复
2. 防止在工作流进行中退出
3. 子代理完成性验证
4. 会话路径写入保护

### 简化的部分 ⚠️
1. 详细的阶段行动指导（从模板化缩减为简单提示）
2. 复杂的上下文分析（依赖 Claude 的智能理解）
3. 多维度决策标准（简化为核心规则）

### 预期影响
- ✅ 启动速度：更快（prompt 更短）
- ✅ 可靠性：更高（不会解析失败）
- ⚠️ 准确性：略有下降（但仍能满足 80%+ 场景）

---

## 备份文件

- `settings.json.backup`：最初的备份（移除 hook 前）
- `settings.json.full-backup`：完整备份（包含原始长 prompt）

如需恢复原配置：
```bash
cp .claude/settings.json.full-backup .claude/settings.json
```

---

## 测试建议

1. **基础测试**：
   ```bash
   claude --dangerously-skip-permissions
   ```
   检查是否正常启动

2. **工作流测试**：
   - 启动一个工作流
   - 中途退出
   - 重新启动，观察是否提示继续执行

3. **Stop Hook 测试**：
   - 在工作流进行中尝试退出
   - 观察是否被拦截

4. **子代理测试**：
   - 调用子代理
   - 观察完成性检查是否工作

---

## 如需进一步优化

如果简化版功能不够用，可以考虑：

1. **将 prompt 拆分为多个小 hook**
2. **用命令型 hook 实现部分规则检查**
3. **使用外部配置文件存储长文本**

---

**优化完成** ✅
